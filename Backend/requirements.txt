mlx
mlx-lm-lora
matplotlib

# OCR / Multimodal family (baidu/Unlimited-OCR) — OPTIONAL.
# Requires the mlx-vlm fork that adds the `unlimited_ocr` model package
# (model_type: unlimited-ocr) and the training fixes. Image-text LoRA training
# and PDF/image OCR inference are verified against this fork. Left commented so
# the default text-training install is unaffected (a plain `mlx-vlm` would pull
# upstream, which lacks the unlimited_ocr package). Also needs pymupdf+pillow for
# PDF OCR (Backend/ocr_infer.py).
# Enable OCR support with:
#   pip install "mlx-vlm @ git+https://github.com/MChorfa/mlx-vlm@feat/unlimited-ocr" pymupdf pillow
# mlx-vlm @ git+https://github.com/MChorfa/mlx-vlm@feat/unlimited-ocr
# pymupdf
# pillow
